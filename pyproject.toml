[build-system]
requires = ["hatchling>=1.27", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "beets-hitlisttag"
description = "A beets plugin that stores chart ('hitlist') history in file tags."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Peter van Liesdonk", email = "peter@liesdonk.nl" }]
keywords = ["beets", "charts", "hitlist", "music", "plugin"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Plugins",
    "Intended Audience :: End Users/Desktop",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Multimedia :: Sound/Audio",
]
dynamic = ["version"]
dependencies = [
    "beets>=2.12",
    "mediafile>=0.17.0",
]

[project.urls]
Homepage = "https://github.com/pvliesdonk/beets-hitlisttag"
Repository = "https://github.com/pvliesdonk/beets-hitlisttag"
Issues = "https://github.com/pvliesdonk/beets-hitlisttag/issues"

[project.optional-dependencies]
test = ["pytest>=8", "ruff"]

# Version is derived from the git tag; no tags yet yields a dev version.
[tool.hatch.version]
source = "vcs"

# The plugin modules live under the PEP 420 `beetsplug` namespace.
[tool.hatch.build.targets.wheel]
packages = ["beetsplug"]
exclude = [
    "beetsplug/hitlisttag.py.backup",
    "beetsplug/hitlisttag.py.old",
    "beetsplug/hitlisttag.py.template_funcs",
]

[tool.ruff]
line-length = 88
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]
